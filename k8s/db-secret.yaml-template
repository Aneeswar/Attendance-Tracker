apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: default
type: Opaque
data:
  # Values must be base64-encoded
  # PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes("your-string"))
  # Linux/macOS: echo -n "your-string" | base64
  username: "ZGJhZG1pbg==" # Base64 for "dbadmin"
  password: "REPLACE_WITH_BASE64_PASSWORD"
